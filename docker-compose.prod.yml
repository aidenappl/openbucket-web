version: '3.8'

services:
  openbucket-web:
    image: ${YOUR_REGISTRY}/openbucket-web:latest
    container_name: openbucket-web
    ports:
      - "3001:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
      HOSTNAME: "0.0.0.0"
    restart: unless-stopped
    networks:
      - openbucket-network
    healthcheck:
      test: 
        - "CMD-SHELL"
        - "curl -f http://localhost:3000/api/health || exit 1"
      interval: 30s
      timeout: 15s
      retries: 3
      start_period: 45s
    depends_on:
      - openbucket-api
    labels:
      - "com.docker.compose.project=openbucket-web"
      - "traefik.enable=false"

  openbucket-api:
    image: ${YOUR_REGISTRY}/openbucket-api:latest
    container_name: openbucket-api
    ports:
      - "8003:8001"
    environment:
      PORT: 8001
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - openbucket-network
    healthcheck:
      test: 
        - "CMD-SHELL"
        - "wget --no-verbose --tries=1 --spider http://localhost:8001/health || exit 1"
      interval: 30s
      timeout: 15s
      retries: 3
      start_period: 30s
    labels:
      - "com.docker.compose.project=openbucket-web"
      - "traefik.enable=false"

networks:
  openbucket-network:
    driver: bridge
    name: openbucket-network